pkgname=cockpit-geeko
pkgver=VERSION
pkgrel=1
pkgdesc='Cockpit Geeko Example Module'
arch=('x86_64')
url='https://github.com/rsimai/cockpit-geeko'
license=(LGPL)
source=("SOURCE")
sha256sums=('SKIP')

package() {
  depends=(cockpit)
  cd $pkgname
  make DESTDIR="$pkgdir" install PREFIX=/usr
}
